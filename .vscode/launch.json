{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Ejecutar programa.exe",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/programa.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "preLaunchTask": "build",
            "MIMode": "gdb",
            "miDebuggerPath": "gdb",
            "setupCommands": [
                {
                    "description": "Habilitar la impresión de variables en formato pretty",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ]
        }
    ]
}
